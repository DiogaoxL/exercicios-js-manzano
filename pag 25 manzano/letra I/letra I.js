// Ler dois inteiros (variáveis A e B) e imprimir o resultado do quadrado da diferença do primeiro valor pelo 
// segundo.

var n1 = parseInt(prompt("digite um valor: "))
var n2 = parseInt(prompt("digite outro valor: "))

diferenca = n1 - n2 
resultado = diferenca * diferenca

alert("o resultado do quadrado da diferença do primeiro valor pelo segundo foi: " + resultado)
